import 'package:flutter/material.dart';

class SearchOne extends StatelessWidget {
  const SearchOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("search one"),
    );
  }
}
